{
  "name": "jira-dash",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "start": "node server/index.js",
    "build": "cd web && npm run build"
  },
  "devDependencies": { "vitest": "^3.0.0" }
}
